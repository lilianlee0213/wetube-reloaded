extends base.pug

block content
    div.watch__container
        div#videoContainer.video__container
            div.video
                video(src="/" + video.fileUrl, autoplay)#video
            div#videoControls.video__controls
                div
                    input(type="range", step='1', value=0, min='0')#timeline
                div.video__controls__container    
                    div
                        span#play
                            i.fa-solid.fa-play
                        span#forward 
                            i.fas.fa-step-forward
                        span#mute
                            i.fas.fa-volume-up
                        input(type='range', step="0.1", value=0.5, min='0', max='1')#volume 
                        div 
                            span#currentTime 00:00
                            span  / 
                            span#totalTime 00:00
                    div
                        span#fullScreen 
                            i.fas.fa-expand
        div.comment__container
            form(action="")
                input(type="textarea")  
        div.moreVideos
            ul 
                li hello
                li hello
                li hello
                li hello
        //- div
        //-     p=video.description
        //-     small=video.createdAt
        //- div 
        //-     small Uploaded by 
        //-         a(href=`/users/${video.creator._id}`)=video.creator.username
        //- if loggedInUser
        //-     if String(video.creator._id) === String(loggedInUser._id)
        //-         a(href=`${video.id}/edit`) Edit Video &rarr; 
        //-         br
        //-         a(href=`${video.id}/delete`) Delete Video &rarr; 
block scripts
    script(src="/static/js/videoPlayer.js") 
        